# ==============================================================================
# Raw INEP microdata -- must never be committed (restricted/individually
# identifiable; see README for where to obtain these directly from INEP).
# Patterns below match the raw filenames this script expects in the working
# directory, so an accidental `git add .` after downloading them is caught.
# ==============================================================================
DM_CURSO_*.CSV
DM_CURSO_*.csv
HEP_*.CSV
HEP_*.csv
DM_DOCENTE_*.CSV
DM_DOCENTE_*.csv
MICRODADOS_ENEM_*.CSV
MICRODADOS_ENEM_*.csv
MICRODADOS_IDD_*.txt
microdados_idd_*.csv
FINANCIAMENTO_CONCEDIDOS_*.csv
CPC_20*.xls
CPC_20*.xlsx

# The original, un-cleaned conglomerates files -- superseded by
# conglomerates_crosswalk.csv/.txt, which are the versions actually tracked.
Conglomerates.xlsx

# ==============================================================================
# Derived/intermediate Stata datasets -- regenerated by fees_scores_benefit.do
# on each run, including Conglomerates.dta itself, which the script rebuilds
# from the tracked conglomerates_crosswalk.csv every time it runs. Excluded
# because these are (a) reproducible from the raw data and code, and (b) some
# retain individual student-level records even after cleaning, which stay
# restricted regardless of processing.
# ==============================================================================
*.dta

# ==============================================================================
# Stata run artifacts
# ==============================================================================
*.log
*.smcl
*.stswp

# ==============================================================================
# Script output -- LaTeX table fragments and figures produced by the .do file.
# Regenerate by running the script rather than tracking these directly;
# comment out if you'd rather keep a reference copy of the output in-repo.
# ==============================================================================
*.tex.fragment
fees_enrollments*.tex
quality_differentiation*.tex
fee_differentiation*.tex
benefit*.tex
quintile_*.tex
quantile_*.tex
HHI.eps

# ==============================================================================
# LaTeX build artifacts (if the paper source is kept in this repository) --
# keep the .tex/.bib source and the final .pdf, ignore everything LaTeX
# regenerates on compile.
# ==============================================================================
*.aux
*.bbl
*.blg
*.out
*.toc
*.synctex.gz
*.fdb_latexmk
*.fls
*.nav
*.snm
*.vrb

# ==============================================================================
# OS and editor junk
# ==============================================================================
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
